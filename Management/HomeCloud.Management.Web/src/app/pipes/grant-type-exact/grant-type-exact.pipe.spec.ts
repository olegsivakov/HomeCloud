import { GrantTypeExactPipe } from './grant-type-exact.pipe';

describe('GrantTypeExactPipe', () => {
  it('create an instance', () => {
    const pipe = new GrantTypeExactPipe();
    expect(pipe).toBeTruthy();
  });
});
